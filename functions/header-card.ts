export function headerCard() {
  return {
    init() {
      // Initialize header card functionality
    },
    updateTitle(_title: string) {
      // Dynamic title updates if needed
      // Using _title to indicate intentionally unused parameter
    }
  };
} 